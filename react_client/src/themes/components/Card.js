
const Card = {
  parts: ['container'],
  baseStyle: {
    container: {
      borderWidth: "1px",
      borderRadius: "5px",
      overflow: "hidden",
      background: '#ffffff',
      boxShadow: '6px 3px 3px rgba(100, 100, 100, .4)',
      height: '100%',
      textAlign: 'center',
    }
  }
}

export default Card
